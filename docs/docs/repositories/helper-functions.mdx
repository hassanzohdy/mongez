---
title: Repository Helper Functions
---

### repo function helper

You can access any repository from anywhere using the `repo` function by passing the repository alais name.

```php title="app\Http\Controllers\UsersControllers.php"
<?php

namespace App\Http\Controllers;

class UsersControllers extends Controller
{
    /**
     * {@inheritDoc}
     */
    public function index()
    {
        $users = repo('users')->list([
            'status' => 'active',
        ]);
    }
}
```
